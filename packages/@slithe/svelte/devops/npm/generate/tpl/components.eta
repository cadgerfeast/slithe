<% it.wrappers.forEach((wrapper) => { %>
import <%= wrapper.pascalName %> from './wrappers/<%= wrapper.name %>.svelte';
<% }) %>

export {
<% it.wrappers.forEach((wrapper) => { %>
  <%= wrapper.pascalName %>,
<% }) %>
};
